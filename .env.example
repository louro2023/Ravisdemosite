# Firebase Configuration (already pre-configured in firebase.ts)
# These are public keys and can be committed to version control

# Application URL
APP_URL="http://localhost:3000"

# Optional: Firebase Admin SDK credentials (only needed for server-side operations)
# FIREBASE_PROJECT_ID="ravis-7620d"
# FIREBASE_PRIVATE_KEY="your-private-key-here"
# FIREBASE_CLIENT_EMAIL="your-service-account-email@project.iam.gserviceaccount.com"
